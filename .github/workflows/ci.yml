name: CI

on:
  pull_request:
    branches: [main]
    paths:
      - 'nonexistent/**'

jobs:
  exit-0:
    name: exit-0-test
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: exit 0